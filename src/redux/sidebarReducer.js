let initialState = {
  friends: [
    { id: 1, name: 'Valerii' },
    { id: 2, name: 'Anastasiia' },
    { id: 3, name: 'Jessica' }
  ]
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
